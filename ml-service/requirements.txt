# Loose lower bounds on purpose: the team is on Python 3.13, and hard pins to
# older minors have no cp313 wheels (pip would try to compile numpy/sklearn).
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
scikit-learn>=1.5
numpy>=2.0
joblib>=1.4
supabase>=2.9
python-dotenv>=1.0
earthengine-api>=1.0
